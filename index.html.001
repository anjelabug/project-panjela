<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Project Panjela</title>
<link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700&family=Crimson+Pro:ital,wght@0,300;0,400;1,300;1,400&family=DM+Mono:wght@300;400&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0f0d0e;
    --surface: #1e1a1b;
    --card: #fdf8f2;
    --card-border: #d4c8b8;
    --gold: #b8962a;
    --gold-light: #e8c84a;
    --text-dark: #1a1410;
    --text-mid: #4a3f35;
    --text-muted: #8a7a6a;
    --accent: #8b3050;
    --accent-light: #c46080;
    --shadow: rgba(0,0,0,0.5);
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    min-height: 100vh;
    font-family: 'Crimson Pro', serif;
    background-color: var(--bg);
    background-image:
      radial-gradient(ellipse at 20% 10%, rgba(139,48,80,0.12) 0%, transparent 50%),
      radial-gradient(ellipse at 80% 90%, rgba(184,150,42,0.08) 0%, transparent 50%);
    padding: 2rem 1rem 4rem;
    color: var(--card);
  }

  /* ── HEADER ── */
  header {
    text-align: center;
    margin-bottom: 2.5rem;
  }

  .header-ornament {
    font-size: 0.65rem;
    letter-spacing: 0.5em;
    color: rgba(255,255,255,0.15);
    margin-bottom: 0.6rem;
    display: block;
  }

  .site-title {
    font-family: 'Cinzel', serif;
    font-size: 2rem;
    font-weight: 700;
    color: var(--gold-light);
    letter-spacing: 0.08em;
    text-shadow: 0 2px 12px rgba(184,150,42,0.3);
    line-height: 1.1;
  }

  .site-subtitle {
    font-family: 'DM Mono', monospace;
    font-size: 0.58rem;
    color: rgba(255,255,255,0.3);
    letter-spacing: 0.2em;
    text-transform: uppercase;
    margin-top: 0.5rem;
  }

  .header-rule {
    width: 60px;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
    margin: 1.2rem auto 0;
  }

  /* ── SECTION LABEL ── */
  .section-label {
    font-family: 'DM Mono', monospace;
    font-size: 0.55rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.25);
    border-bottom: 1px solid rgba(255,255,255,0.08);
    padding-bottom: 0.4rem;
    margin-bottom: 0.75rem;
  }

  /* ── CHALLENGE CARD ── */
  /*
    TO ADD A NEW LINK: Copy everything between
    <!--CARD START--> and <!--CARD END-->
    and paste it below the last card.
    Update the href, the challenge name, the description, and the status badge.
  */

  .card {
    background: var(--card);
    border: 1px solid var(--card-border);
    border-radius: 6px;
    margin-bottom: 0.75rem;
    box-shadow: 0 4px 20px var(--shadow);
    overflow: hidden;
    text-decoration: none;
    display: block;
    transition: transform 0.15s, box-shadow 0.15s;
    -webkit-tap-highlight-color: transparent;
  }

  .card:active {
    transform: scale(0.985);
    box-shadow: 0 2px 10px var(--shadow);
  }

  .card-inner {
    display: flex;
    align-items: stretch;
  }

  /* colored left stripe */
  .card-stripe {
    width: 5px;
    flex-shrink: 0;
    background: linear-gradient(180deg, var(--accent), var(--accent-light));
  }

  .card-stripe.gold {
    background: linear-gradient(180deg, var(--gold), var(--gold-light));
  }

  .card-stripe.dark {
    background: linear-gradient(180deg, #2a2a2a, #5a5050);
  }

  .card-body {
    flex: 1;
    padding: 0.9rem 1rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
  }

  .card-text { flex: 1; min-width: 0; }

  .card-eyebrow {
    font-family: 'DM Mono', monospace;
    font-size: 0.5rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--text-muted);
    margin-bottom: 0.2rem;
  }

  .card-title {
    font-family: 'Cinzel', serif;
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--text-dark);
    line-height: 1.2;
  }

  .card-desc {
    font-size: 0.8rem;
    color: var(--text-mid);
    font-style: italic;
    margin-top: 0.2rem;
    line-height: 1.3;
  }

  .card-arrow {
    font-size: 1rem;
    color: var(--text-muted);
    flex-shrink: 0;
    padding-right: 0.2rem;
  }

  /* status badge */
  .badge {
    display: inline-block;
    font-family: 'DM Mono', monospace;
    font-size: 0.48rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    padding: 0.15rem 0.5rem;
    border-radius: 2px;
    margin-top: 0.35rem;
  }

  .badge-active {
    background: #eef5f1;
    color: #3d6b52;
    border: 1px solid #b8d4c4;
  }

  .badge-complete {
    background: #f7f2e8;
    color: #8b6b2a;
    border: 1px solid #d4c4a0;
  }

  .badge-upcoming {
    background: #f0ede8;
    color: #8a7a6a;
    border: 1px solid #d4c8b8;
  }

  /* ── FOOTER ── */
  footer {
    text-align: center;
    margin-top: 3rem;
    font-family: 'DM Mono', monospace;
    font-size: 0.52rem;
    color: rgba(255,255,255,0.15);
    letter-spacing: 0.1em;
  }
</style>
</head>
<body>

<header>
  <span class="header-ornament">♠ ♥ ♣ ♦</span>
  <div class="site-title">Project Panjela</div>
  <div class="site-subtitle">Panning Challenges · 2026</div>
  <div class="header-rule"></div>
</header>

<div class="section-label">Active Challenges</div>

<!--CARD START-->
<a class="card" href="https://anjelabug.github.io/project-panjela/deckofpanning.html">
  <div class="card-inner">
    <div class="card-stripe"></div>
    <div class="card-body">
      <div class="card-text">
        <div class="card-eyebrow">Community Challenge</div>
        <div class="card-title">Deck of Panning · 2026</div>
        <div class="card-desc">Five cards · Wildcard, Gifted, Set, Long Haul, Are You Joking?!</div>
        <span class="badge badge-active">● Active</span>
      </div>
      <div class="card-arrow">›</div>
    </div>
  </div>
</a>
<!--CARD END-->

<!--CARD START-->
<a class="card" href="project-panopoly.html">
  <div class="card-inner">
    <div class="card-stripe"></div>
    <div class="card-body">
      <div class="card-text">
        <div class="card-eyebrow">Community Challenge</div>
        <div class="card-title">Project Panopoly · 2nd Quarter 2026</div>
        <div class="card-desc">Board game challenge: Collect cards by using items in your collection</div>
        <span class="badge badge-active">● Active</span>
      </div>
      <div class="card-arrow">›</div>
    </div>
  </div>
</a>
<!--CARD END-->


<!-- 
  ═══════════════════════════════════════════════
  TO ADD A NEW CHALLENGE: Copy the block below,
  paste it after the last <!- -CARD END- -> above,
  and update the href, eyebrow, title, desc, and badge.
  ═══════════════════════════════════════════════

<!- -CARD START- ->
<a class="card" href="YOUR-LINK-HERE">
  <div class="card-inner">
    <div class="card-stripe"></div>
    <div class="card-body">
      <div class="card-text">
        <div class="card-eyebrow">Challenge Type</div>
        <div class="card-title">Challenge Name</div>
        <div class="card-desc">Brief description of the challenge</div>
        <span class="badge badge-active">● Active</span>
      </div>
      <div class="card-arrow">›</div>
    </div>
  </div>
</a>
<!- -CARD END- ->

  Change badge-active to badge-complete for finished challenges,
  or badge-upcoming for planned ones.
  Change card-stripe color by adding class="card-stripe gold" or "card-stripe dark"
-->

<footer>
  anjelabug.github.io/project-panjela
</footer>

</body>
</html>
